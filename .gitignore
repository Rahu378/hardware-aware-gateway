.venv/
__pycache__/
*.py[cod]
.pytest_cache/

# Profiler captures are large binaries; commit only the ones you reference.
# The JSON summary is small and worth keeping, the chrome trace is not.
profiles/*
!profiles/.gitkeep
!profiles/*_summary.json

.DS_Store
