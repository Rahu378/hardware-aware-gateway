# Core. Triton ships with the Linux CUDA wheels of torch; on macOS it is absent
# and the CUDA half of the suite skips itself.
torch>=2.4
numpy>=1.24
pytest>=8.0

# Apple silicon only. Harmless to omit elsewhere.
mlx>=0.20 ; platform_system == "Darwin" and platform_machine == "arm64"
